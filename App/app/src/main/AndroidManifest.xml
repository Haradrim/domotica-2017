<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="howest.nmct.be.securitysystem">

    <uses-permission android:name="android.permission.INTERNET" />

    <android:uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <android:uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <android:uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".View.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".View.AddCode"
            android:label="@string/title_activity_add_code"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".View.CommandActivity"
            android:label="@string/title_activity_command"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".View.AllCodesActivity"
            android:label="@string/title_activity_all_codes"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".View.QRActivity"
            android:label="@string/title_activity_qr"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>